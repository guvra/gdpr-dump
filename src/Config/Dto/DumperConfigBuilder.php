<?php

declare(strict_types=1);

namespace Smile\GdprDump\Config\Dto;

class DumperConfigBuilder
{
    public function build(array $data)
    {
        // TODO (generate the dumper object, but validation must be done in the setters, e.g. queries)
    }
}
